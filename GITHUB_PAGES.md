# GitHub Pages deployment

Generated by Prompt 2 on 2026-04-23.

## Where the page lives

- Repository: https://github.com/Victoria-kob/colab-notebooks
- Online webpage: https://victoria-kob.github.io/colab-notebooks/
- Pages source: `main` branch, `/docs` folder.
- Build status at end of Prompt 2: built

## What Prompt 2 changed in this workspace

- Created `docs/` and copied `V-Dem_Plot_With_Conclusion.html`, `gapminder_dashboard.html` into it.
- Created `docs/index.html` as a copy of `V-Dem_Plot_With_Conclusion.html` so GitHub Pages can serve it as the landing page.
- Created `.gitignore` with default rules for macOS, Python, Node, secrets, large data.
- Added `V-Dem-CY-Core-v16.csv` and `Copy of GEDEvent_v25_1.csv` to `.gitignore` because they exceed GitHub's 100 MB file size limit.
- Initialized git repo on `main` branch.
- Configured git user as `Victoria-kob <Victoria-kob@users.noreply.github.com>`.
- Created public GitHub repo `Victoria-kob/colab-notebooks` and pushed.
- Enabled GitHub Pages from `/docs` on the `main` branch.

## What Prompt 2 did NOT change

- Your `README.md` and every existing file in the project were not modified.
- Your `.py`, `.ipynb`, `.txt`, `.csv`, and `data/` files are untouched.
- Files were copied into `docs/`, not moved. Your root HTML files still exist where they were.
- The large CSV files (`V-Dem-CY-Core-v16.csv`, `Copy of GEDEvent_v25_1.csv`) remain on your disk but are excluded from git tracking.

## How to update the page later

Edit any file, then ask Copilot: *"Commit and push."* The online page rebuilds on its own.

## How to undo

- Disable Pages in the repo settings, or delete the `/docs/` folder and push.
- Delete the repository: `gh repo delete Victoria-kob/colab-notebooks`.

## Questions

Ask Copilot (me) about any step above. I can explain why each command was run, or help adjust the setup for your specific project.
